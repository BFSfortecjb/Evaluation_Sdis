// pdf.js — LES DOCUMENTS GÉNÉRÉS (V2).
// Prévu ici : livret de suivi des compétences, fiche individuelle de suivi,
// chevalet, fiche de résolution (livrable 6), fiche d'ajournement (livrable 7).
// Ce fichier évoluera souvent — une erreur ici ne casse jamais la connexion ni la saisie.

function genererLivretCertification(stagiaireId) {
  toast('Génération du livret : prévue en V2', false);
}

function genererFicheSuivi(stagiaireId) {
  toast('Génération de la fiche de suivi : prévue en V2', false);
}
